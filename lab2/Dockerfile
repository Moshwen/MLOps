# Используйем официальную среду выполнения Python в качестве родительского образа
FROM python:3.8-slim

# Установливаем рабочий каталог в контейнере
WORKDIR /app

# Скопируйем текущее содержимое каталога в контейнер
COPY . /app

# Установливаем необхимые библиотеки
RUN pip install --no-cache-dir -r requirements.txt

# Делаем порт 80 доступно
EXPOSE 80

# Определить переменную среды
ENV NAME World

# Открыть app.py когда контейнер запустится
CMD ["python", "app.py"]